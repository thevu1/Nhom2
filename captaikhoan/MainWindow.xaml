<Window x:Class="CapTaiKhoan.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cấp tài khoản"
        Height="350"
        Width="600"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,5">
            <TextBlock Text="User:" Width="100"/>
            <TextBox x:Name="txtUser" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBlock Text="Password:" Width="100"/>
            <PasswordBox x:Name="txtPass" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,5">
            <TextBlock Text="Role:" Width="100"/>
            <ComboBox x:Name="cbRole" Width="200" SelectedIndex="0">
                <ComboBoxItem Content="Admin"/>
                <ComboBoxItem Content="User"/>
            </ComboBox>
        </StackPanel>

        <Button Content="Thêm"
                Grid.Row="3"
                Width="100"
                Margin="0,10"
                Click="BtnAdd_Click"/>

        <DataGrid x:Name="dgUsers"
                  Grid.Row="4"
                  AutoGenerateColumns="True"/>
    </Grid>
</Window>