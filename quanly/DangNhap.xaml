<Window x:Class="group.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập" Height="250" Width="400">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,10" Grid.Row="0">
            <Label Content="Tên đăng nhập:" Width="120"/>
            <TextBox x:Name="txtUsername" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10" Grid.Row="1">
            <Label Content="Mật khẩu:" Width="120"/>
            <PasswordBox x:Name="txtPassword" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,20" Grid.Row="2" HorizontalAlignment="Center">
            <Button Content="Đăng nhập" Width="100" Margin="10" Click="BtnLogin_Click"/>
            <Button Content="Quên mật khẩu" Width="100" Margin="10" Click="BtnForget_Click"/>
            <Button Content="Thoát" Width="100" Margin="10" Click="BtnExit_Click"/>
        </StackPanel>
    </Grid>
</Window>